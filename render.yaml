services:
  - type: web
    name: rrratcoder
    env: node
    buildCommand: npm ci && npm run build
    startCommand: node dist/web/server.js
    plan: free
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
